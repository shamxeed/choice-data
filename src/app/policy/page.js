'use client';

import Layout from '@/client/layouts/Layout';
import Policy from '@/client/components/policy';

const Page = () => {
  return (
    <Layout noHeader>
      <Policy />
    </Layout>
  );
};

export default Page;
