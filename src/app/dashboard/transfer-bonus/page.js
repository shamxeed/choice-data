'use client';

import Layout from '@/client/layouts/Layout';
import TransferBonus from '@/client/components/dashboard/sub-modules/transfer-bonus';

const Page = () => {
  return (
    <Layout>
      <TransferBonus />
    </Layout>
  );
};

export default Page;
