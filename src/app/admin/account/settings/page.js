import Layout from '@/client/layouts/Layout';
import AccountSettings from '@/client/components/admin/account/settings';

const Page = () => {
  return (
    <Layout>
      <AccountSettings />
    </Layout>
  );
};

export default Page;
