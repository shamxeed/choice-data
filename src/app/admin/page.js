'use client';

import Layout from '@/client/layouts/Layout';
import Admin from '@/client/components/admin';

const Page = () => {
  return (
    <Layout>
      <Admin />
    </Layout>
  );
};

export default Page;
