'use client';

import Terms from '@/client/components/terms';
import Layout from '@/client/layouts/Layout';

const Page = () => {
  return (
    <Layout noHeader>
      <Terms />
    </Layout>
  );
};

export default Page;
